{
  "title": "colorCameraParameters.v1",
  "type": "object",
  "properties": {
    "zoomVisibleCamera": {
      "type": "number",
      "maximum": 100,
      "minimum": 0
    }
  },
  "required": [
    "zoomVisibleCamera"
  ]
}